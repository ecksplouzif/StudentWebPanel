server{
        listen 80;
        server_name grouphub.pp.ua;
        location /{
                proxy_pass http://localhost:8080/;

                proxy_set_header Host $host;
                proxy_set_header X-Forwarded-Proto $scheme;
        }
}